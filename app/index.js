// Arrais, objetos
let meta = {
  value: "ler um livro por mês",
  checked: false,
  isChecked: (info) => {
    console.log(info)
  }
}
meta.isChecked(meta.value)

// Function // Arrow function
// function criarMetas() {}

// const criarMetas = () => {}
